interface:
  display_name: "Orchestrate Development V3"
  short_description: "Terra High root with autonomous Astra Low workers"
  default_prompt: "Use $orchestrate-development-v3 to execute this substantial stage with the preferred Terra High root, exact Astra Low implementation workers, bounded Luna XHigh repair, optional Terra High evidence gates, risk-weighted testing, and internal Sol Medium review and Luna XHigh final validation. Luna Max remains a supported compatibility root during migration."

policy:
  allow_implicit_invocation: false
